" Set line numbers
set number

" Enable auto indentation
set smartindent

" Enable a single tab character that appears 4-spaces-wide for each indent
set tabstop=4 softtabstop=0 noexpandtab shiftwidth=4

" Enable syntax highlighting
syntax on
